# Figma Node 939:533 Summary

Source: `https://www.figma.com/design/wLFfFkFzw4zZrUlahUIec6/Perfil-de-viagens--Community-?node-id=939-533&t=jCszvrHlR9rV2SFH-0`

This node is a large rounded container with a pure black background (`#000000`), `80px` internal padding, `20px` corner radius, and a vertical stack layout with `48px` gaps between major sections.

The first section is a single left-aligned heading, `Imagens`, set in Plus Jakarta Sans Medium at `40px` with a `1.4` line-height and light gray text (`#E1E1E6`). It sits above a photo gallery that uses a wrapping layout with `24px` gaps. In the current composition, the gallery shows 12 square thumbnails at `215x215`, arranged as 4 columns by 3 rows. The images are tightly cropped, edge-to-edge, and have no visible borders or corner rounding, which keeps the grid clean and dense.

The final section is a full-width note/callout card below the gallery. It uses a very dark gray fill (`#121214`), `16px` padding, and an `8px` radius. The copy is set in Plus Jakarta Sans at `16px` with a `1.6` line-height and medium-light gray text (`#C4C4CC`). Two inline emphasis treatments stand out: `Unsplash` is bold and underlined like a link, and `Export` is bold italic. Structurally, the whole layout is simple and deliberate: title, image grid, and a supporting instruction block, with the colorful travel photos acting as the main visual focus against the black canvas.
